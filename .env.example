# Public browser values. These are safe to expose in a Vite client when Row Level Security is configured.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# AI provider keys belong ONLY in Supabase Edge Function secrets, never in VITE_* variables.
